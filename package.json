{
  "name": "learn-source-tour",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [],
  "scripts": {
    "build:utils": "pnpm run --filter @xiaoyu/utils build",
    "build:reactivity": "pnpm run --filter @xiaoyu/reactivity build",
    "build": "pnpm run --filter=\"@xiaoyu/*\" --filter='xiaoyu' build",
    "test": "vitest .",
    "test:once": "vitest run .",
    "coverage": "vitest run --coverage",
    "lint": "eslint ./",
    "lint:fix": "eslint ./ --fix"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.29.4",
    "@vitest/coverage-c8": "^0.25.0",
    "@vitest/coverage-istanbul": "^0.25.0",
    "eslint": "^8.27.0",
    "typescript": "^4.8.4",
    "unbuild": "^0.9.4",
    "vitest": "^0.25.0"
  }
}
